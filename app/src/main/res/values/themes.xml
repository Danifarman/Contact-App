<resources>
    <!-- Base application theme -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Custom Popup Menu Style -->
    <style name="CustomPopupMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@color/black</item> <!-- Popup background color -->
        <item name="android:textColor">@color/white</item> <!-- Popup text color -->
        <item name="android:overlapAnchor">true</item> <!-- Ensure popup does not overlap toolbar -->
    </style>

    <!-- Theme Overlay for Popup Menu -->
    <style name="ThemeOverlay.PopupMenu" parent="ThemeOverlay.AppCompat.Dark">
        <item name="popupMenuStyle">@style/CustomPopupMenu</item>
    </style>

    <!-- Your existing styles -->
    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication">
        <!-- Customizations -->
    </style>
</resources>
